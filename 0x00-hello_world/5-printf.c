#include <stdio.h>
/**
 * main - prints "with proper grammer, but the outcome is a piece of art," followed newline
 * Return: Always 0,
 *
 */

int main(void)
{
printf("w0ith proper grammar, but the outcome is a piece of art,\n");
return (0);
}
